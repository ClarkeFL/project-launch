name: Build and Release

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0
  workflow_dispatch:  # Allow manual trigger
    inputs:
      create_release:
        description: 'Create a GitHub release'
        required: false
        default: 'false'
        type: boolean

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: ProjectLauncher-Setup-*.exe
            asset_name: ProjectLauncher-Setup-Windows
          - os: macos-latest
            artifact_name: ProjectLauncher-Installer-*.dmg
            asset_name: ProjectLauncher-Installer-macOS
          - os: ubuntu-latest
            artifact_name: project-launcher-installer-*
            asset_name: project-launcher-installer-Linux

    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      # macOS: Create .icns icon file
      - name: Create macOS icon
        if: matrix.os == 'macos-latest'
        run: |
          python create_icons.py
          
      # Windows/Linux: Create icons
      - name: Create icons
        if: matrix.os != 'macos-latest'
        run: |
          python create_icons.py

      - name: Build installer
        run: python build_installer.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.artifact_name }}
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') || github.event.inputs.create_release == 'true'
    
    permissions:
      contents: write
      
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -laR artifacts/

      - name: Get version from tag
        id: get_version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "version=dev-$(date +%Y%m%d-%H%M%S)" >> $GITHUB_OUTPUT
          fi

      - name: Prepare release files
        run: |
          mkdir -p release
          VERSION="${{ steps.get_version.outputs.version }}"
          # Move and rename files for release with version in filename
          find artifacts/ProjectLauncher-Setup-Windows -name "*.exe" -exec mv {} "release/ProjectLauncher-Setup-Windows-${VERSION}.exe" \; 2>/dev/null || true
          find artifacts/ProjectLauncher-Installer-macOS -name "*.dmg" -exec mv {} "release/ProjectLauncher-Installer-macOS-${VERSION}.dmg" \; 2>/dev/null || true
          find artifacts/project-launcher-installer-Linux -type f -exec mv {} "release/project-launcher-installer-Linux-${VERSION}" \; 2>/dev/null || true
          # Make Linux installer executable
          chmod +x release/project-launcher-installer-Linux-* || true
          ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: Project Launcher ${{ steps.get_version.outputs.version }}
          draft: false
          prerelease: ${{ !startsWith(github.ref, 'refs/tags/v') }}
          generate_release_notes: true
          files: |
            release/ProjectLauncher-Setup-Windows-${{ steps.get_version.outputs.version }}.exe
            release/ProjectLauncher-Installer-macOS-${{ steps.get_version.outputs.version }}.dmg
            release/project-launcher-installer-Linux-${{ steps.get_version.outputs.version }}
          body: |
            ## Project Launcher ${{ steps.get_version.outputs.version }}
            
            ### Downloads
            
            | Platform | File | Instructions |
            |----------|------|--------------|
            | **Windows** | `ProjectLauncher-Setup-Windows-${{ steps.get_version.outputs.version }}.exe` | Double-click to install |
            | **macOS** | `ProjectLauncher-Installer-macOS-${{ steps.get_version.outputs.version }}.dmg` | Open DMG, run the installer inside |
            | **Linux** | `project-launcher-installer-Linux-${{ steps.get_version.outputs.version }}` | Download, run `chmod +x` and execute |
            
            ### What's Included
            
            - One-click project launching
            - Support for VS Code, Cursor, Zed, Windsurf, and JetBrains IDEs
            - AI coding tools integration (OpenCode, Claude Code, Aider)
            - Multi-browser support
            - System tray icon
            - Auto-start on login (configurable from Settings)
            
            ### Installation
            
            **Windows:** Just double-click the `.exe` file and follow the prompts.
            
            **macOS:** 
            1. Open the `.dmg` file
            2. Run the installer inside
            3. If blocked by Gatekeeper, right-click and select "Open"
            
            **Linux:** 
            ```bash
            chmod +x ./project-launcher-installer-Linux-${{ steps.get_version.outputs.version }}
            ./project-launcher-installer-Linux-${{ steps.get_version.outputs.version }}
            ```
